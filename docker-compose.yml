version: "3.8"

services:

  backend:
    build: ./backend
    container_name: fastapi_backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend/frontend
    container_name: react_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend